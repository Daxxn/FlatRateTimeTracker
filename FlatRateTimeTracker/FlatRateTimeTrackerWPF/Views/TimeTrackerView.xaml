<UserControl x:Class="FlatRateTimeTrackerWPF.Views.TimeTrackerView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:FlatRateTimeTrackerWPF.Views"
             mc:Ignorable="d" Background="Wheat"
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid>
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="Auto"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>

		<Border Grid.Column="0">
			<Grid>
				<Grid.RowDefinitions>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
					<RowDefinition Height="Auto"/>
				</Grid.RowDefinitions>
				<TextBlock>Test Window</TextBlock>
				<Button x:Name="TestBuildTree" Grid.Row="1">Build Test Tree</Button>

				<Border Grid.Row="2" Margin="0,10,0,0">
					<Grid>
						<Grid.RowDefinitions>
							<RowDefinition Height="Auto"/>
							<RowDefinition Height="Auto"/>
						</Grid.RowDefinitions>

						<TextBlock>File Control</TextBlock>

						<Grid Grid.Row="1">
							<Grid.RowDefinitions>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
								<RowDefinition Height="Auto"/>
							</Grid.RowDefinitions>
							<TextBlock>Open File Path</TextBlock>
							<TextBox Text="{Binding Path=FilePath}" Grid.Row="1"/>
							<Button x:Name="OpenFileButton" Grid.Row="2">Open File</Button>
							<Button x:Name="SaveFileButton" Grid.Row="3">Save File</Button>
							<Button x:Name="SaveFileTestButton" Grid.Row="4">Save Test</Button>
						</Grid>
					</Grid>
				</Border>

				<Border Grid.Row="3">
					<StackPanel Orientation="Vertical">
						<Button x:Name="AddROsButton">Add Multiple ROs</Button>
					</StackPanel>
				</Border>
			</Grid>
		</Border>

		<Border Grid.Column="1">
			<TreeView Grid.IsSharedSizeScope="True" x:Name="TimeTreeViewer">
				<StackPanel Orientation="Vertical">
					<TreeViewItem Header="Day" ItemsSource="{Binding Path=TimeController.Data}">
						<TreeViewItem.ItemTemplate>
							<DataTemplate>
								<StackPanel Orientation="Vertical">
									<TextBlock Text="{Binding Path=Date}"/>
									<StackPanel Orientation="Vertical">
										<TreeViewItem Header="Jobs" ItemsSource="{Binding Path=Jobs}">
											<TreeViewItem.ItemTemplate>
												<DataTemplate>
													<Border Style="{StaticResource TT_Job_Border}">
														<Grid>
															<Grid.ColumnDefinitions>
																<ColumnDefinition SharedSizeGroup="Titles"/>
																<ColumnDefinition SharedSizeGroup="Values"/>
															</Grid.ColumnDefinitions>
															<Grid.RowDefinitions>
																<RowDefinition Height="Auto"/>
																<RowDefinition Height="Auto"/>
																<RowDefinition Height="Auto"/>
																<RowDefinition Height="Auto"/>
																<RowDefinition Height="Auto"/>
															</Grid.RowDefinitions>
															<TextBlock Grid.Row="0">Job Type</TextBlock>
															<TextBox Text="{Binding Path=Type}" Grid.Row="0" Grid.Column="1"/>
															<TextBlock Grid.Row="1">RO Num</TextBlock>
															<TextBox Text="{Binding Path=RONumber}" Grid.Row="1" Grid.Column="1"/>
															<TextBlock Grid.Row="2">Hours</TextBlock>
															<TextBox Text="{Binding Path=FlaggedHours}" Grid.Row="2" Grid.Column="1"/>
															<TextBlock Grid.Row="3">Time</TextBlock>
															<TextBox Text="{Binding Path=OutTime}" Grid.Row="3" Grid.Column="1"/>
														</Grid>
													</Border>
												</DataTemplate>
											</TreeViewItem.ItemTemplate>
										</TreeViewItem>
										<StackPanel Orientation="Horizontal">
											<Button Click="AddJob_Click">Add Job</Button>
										</StackPanel>
									</StackPanel>
								</StackPanel>
							</DataTemplate>
						</TreeViewItem.ItemTemplate>
					</TreeViewItem>
					<StackPanel Orientation="Horizontal">
						<Button Click="AddDay_Click" Margin="0,0,20,0">Add Day</Button>
						<Button Click="DelDay_Click">Del Day</Button>
						<StackPanel Orientation="Horizontal">
							<TextBox Text="{Binding Path=DayIndex, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" MinWidth="10" MaxLength="3"/>
							<StackPanel Orientation="Vertical">
								<Button x:Name="DayUp" Click="DayIndex_Click" FontSize="7">/\</Button>
								<Button x:Name="DayDown" Click="DayIndex_Click" FontSize="7">\/</Button>
							</StackPanel>
						</StackPanel>
						<Button Click="DelJob_Click">Del Job</Button>
						<StackPanel Orientation="Horizontal">
							<TextBox Text="{Binding Path=JobIndex, UpdateSourceTrigger=PropertyChanged, Mode=TwoWay}" MinWidth="10" MaxLength="3"/>
							<StackPanel Orientation="Vertical">
								<Button x:Name="JobUp" Click="JobIndex_Click" FontSize="7">/\</Button>
								<Button x:Name="JobDown" Click="JobIndex_Click" FontSize="7">\/</Button>
							</StackPanel>
						</StackPanel>
					</StackPanel>
				</StackPanel>
			</TreeView>
		</Border>
    </Grid>
</UserControl>
